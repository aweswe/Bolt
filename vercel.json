{
  "installCommand": "corepack enable && corepack prepare pnpm@9.12.3 --activate && pnpm install --frozen-lockfile=false",
  "buildCommand": "pnpm turbo run build --filter=@bolt/web",
  "framework": "nextjs"
}
